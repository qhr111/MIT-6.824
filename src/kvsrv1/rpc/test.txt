                ┌─────────────────────────────┐
                │       多模态时空数据输入       │
                │ (激光/图像/天气等)           │
                └────────────┬─────────────── ┘
                             │
                             ▼
                ┌──────────────────────────────────────────┐
                │   动态时空图构建模块 (Dynamic Graph)        │
                │ • 基于时变邻接矩阵A(t)或相关性算法计算动态拓扑  │
                │ • 节点 = 各时空采样点；边权=相关性            │
                └────────────┬─────────────────────────────┘
                             │
                             ▼
       ┌──────────────────────────────────────────────────┐
       │            时空特征提取模块 (ST Encoder)            │
       │ ┌──────────────────────────────────────────────┐ │
       │ │ ① Temporal GRU / Transformer                │ │
       │ │    学习时间依赖 (短期/长期趋势)                  │ │
       │ │ ② Spatial GAT                               │ │
       │ │    学习动态图节点间空间相关性                    │ │
       │ └──────────────────────────────────────────────┘ │
       │ 输出: 时空嵌入特征 F_t , S_t                       │
       └────────────┬─────────────────────────────────────┘
                     │
                     ▼
       ┌────────────────────────────────────────────┐
       │   跨模态注意力融合模块 (Cross-Modal Attention) │
       │  • 多模态Query-Key交互 (视觉/环境/激光模态)     │
       │  • 融合特征 Z = Attention(F_t ⊕ S_t ⊕ M_t)   │
       └────────────┬───────────────────────────────┘
                     │
                     ▼
       ┌────────────────────────────────────────────┐
       │        条件GAN插补模块 (cGAN Imputer)        │
       │ Generator: GRU-GAT条件生成器                 │
       │ Discriminator: 多模态判别器 D(X, M_t, cond)  │
       │ 目标: 学习 P(X_real | X_masked, cond)        │
       └─────────────────────────────────────────────┘